syntax = "proto3";

package scheduler.worker;

import weak "gogoproto/gogo.proto";
import "api/utils/empty.proto";

option go_package = "github.com/silverswords/scheduler/api/worker";

service Worker {
    rpc Run(RunRequest) returns (scheduler.utils.Empty);
    rpc CancelTask(CancelRequest) returns (scheduler.utils.Empty);
}

message RunRequest {
    string TaskName = 1 [(gogoproto.nullable) = false];
}

message CancelRequest {
    string TaskName = 1 [(gogoproto.nullable) = false];
}